services:
  - type: web
    name: resume-matcher-backend
    runtime: docker
    region: singapore
    plan: free
    rootDir: apps/backend
    dockerfilePath: Dockerfile.render
    healthCheckPath: /api/v1/health

    envVars:
      - key: LLM_PROVIDER
        value: nvidia
      - key: LLM_MODEL
        value: meta/llama-3.1-8b-instruct
      - key: FRONTEND_BASE_URL
        value: https://resume-matcher-zeta.vercel.app
      - key: CORS_ORIGINS
        value: '["https://resume-matcher-zeta.vercel.app","https://resume-matcher.vercel.app","http://localhost:3000"]'
      - key: LOG_LEVEL
        value: INFO
      - key: REQUEST_TIMEOUT_SECONDS
        value: "120"
      # LLM_API_KEY and DATABASE_URL must be set in the Render dashboard (not here)
